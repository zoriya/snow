apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sish-tls
spec:
  secretName: sish-tls
  dnsNames:
    - "*.tun.sdg.moe"
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
