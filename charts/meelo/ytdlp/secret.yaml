apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: yt-playlist
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: bitwarden
    kind: ClusterSecretStore
  data:
  - secretKey: url
    remoteRef:
      key: yt-playlist
